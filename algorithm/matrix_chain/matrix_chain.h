#ifndef MATRIX_CHAIN_H_
#define MATRIX_CHAIN_H_

#include <algorithm>
#include <map>
#include <tuple>
#include <vector>
using std::tuple;
using std::vector;
using std::get;

using T = long long; 
T matrix_chain(const vector<T>& mat_info);
#endif
